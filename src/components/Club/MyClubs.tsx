import type { FC } from 'react';
import {
	Avatar,
	Box,
	Button,
	Chip,
	IconButton,
	Stack,
	SvgIcon,
	TextField,
	Typography,
} from '@mui/material';

export const MyClubs: FC = () => {
	return <Box sx={{ p: 3 }}>MyClubs</Box>;
};
